---
title: '【完全自動化】Zennブログ執筆環境構築｜GitHub連携×textlint×Prettier×Copilot'
emoji: '🔰'
type: 'tech'
topics: ['Zenn', 'Copilot', 'textlint', 'Prettier', 'AI']
published: true
---

## この記事の対象読者

この記事は、以下のようなほうに向けて書いています。

- Zennでブログを始めたいが、まだGitHub連携していないほう
- 記事の品質を保ちながら効率的に管理したいほう
- AIを活用して記事作成を楽にしたい方
- textlintやPrettierを使った自動校正に興味があるほう

## はじめに

Zennでブログを書こうと思ったとき、こんな悩みを感じたことはないでしょうか。

- 「記事を書いたけど、日本語の表現が適切か不安」
- 「コードのフォーマットがバラバラで見づらい」
- 「コミット前にチェックし忘れて、後で修正が必要になった」
- 「AIを使って効率的に記事を書きたいけど、どう設定すればいいかわからない」

この記事では、こうした悩みを解決するための環境構築方法を紹介します。

具体的には、以下のような環境を整えることで、記事の品質を保ちながら効率的に執筆できるようになります。

- **記事の品質向上**: textlintによる日本語の自動校正
- **フォーマットの統一**: Prettierによる自動整形
- **コミット時の自動チェック**: huskyとlint-stagedによる品質保証
- **AIによる執筆支援**: GitHub Copilotとの連携

手順に沿って進めるだけで構築できるよう、わかりやすく解説します。

## 完成イメージ

この記事の手順を完了すると、以下のような環境が構築できます。

```text
zenn-blog/
├── .github/
│   ├── copilot-instructions.md  # GitHub Copilot用プロジェクトルール
│   └── prompts/                 # GitHub Copilot用プロンプト集
├── .husky/
│   └── pre-commit               # コミット前の自動チェック
├── articles/                    # Zenn記事
├── books/                       # Zenn本
├── images/                      # 画像ファイル
├── .textlintrc.json             # textlint設定
├── .prettierrc.cjs              # Prettier設定
└── package.json                 # パッケージ管理
```

## Step 1: GitHubとの連携

### 1-1. GitHubリポジトリーを作成する

まず、GitHubで新しいリポジトリーを作成します。

![GitHubリポジトリ作成画面](/images/setup-zenn-blog-with-best-practices/zenn-blog-repository-creation.png)

1. GitHubにログイン
2. 右上の「+」から「New repository」を選択
3. リポジトリー名を入力（例： `zenn-blog`）
4. 公開設定を選択（PublicまたはPrivate）
5. 「Create repository」をクリック

### 1-2. Zennダッシュボードから連携する

次に、ZennとGitHubを連携します。

1. [Zennダッシュボード](https://zenn.dev/dashboard)にアクセス
2. 「GitHubリポジトリー連携」を選択
   :::details ZennダッシュボードのGitHub連携画面
   ![ZennダッシュボードのGitHub連携画面](/images/setup-zenn-blog-with-best-practices/zenn-github-integration.png)
   ![ZennダッシュボードのGithub連携ポップアップ](/images/setup-zenn-blog-with-best-practices/zenn-github-repository-popup.png)
   :::
3. 作成したリポジトリーを選択
   :::details Zennダッシュボードのリポジトリー選択画面
   ![Zennダッシュボードのリポジトリ選択画面](/images/setup-zenn-blog-with-best-practices/zenn-select-repository.png)
   :::
4. 連携を承認

### 1-3. 同期するブランチ名を確認

デフォルトでは`main`ブランチが同期対象になります。

- Zennの設定画面で同期ブランチを確認
- 必要に応じて変更可能

## Step 2: リポジトリのパッケージ化

### 2-1. ローカルにリポジトリをクローン

```bash
git clone https://github.com/[your-user-name]/zenn-blog.git
cd zenn-blog
```

### 2-2. npm初期化

```bash
npm init -y
```

`package.json`が作成されます。

### 2-3. 必要なライブラリをインストール

```bash
# 依存パッケージ（dependencies）
npm install textlint-rule-ja-hiragana-fukushi textlint-rule-ja-hiragana-hojodoushi textlint-rule-ja-hiragana-keishikimeishi textlint-rule-ja-no-redundant-expression textlint-rule-ja-no-weak-phrase textlint-rule-no-doubled-joshi textlint-rule-preset-jtf-style textlint-rule-prh zenn-cli

# 開発用パッケージ（devDependencies）
npm install -D eslint-config-prettier husky lint-staged prettier textlint textlint-rule-no-todo
```

### 各ライブラリーの説明

::: details textlint関連ルール

| パッケージ名                               | 役割                                                                        |
| ------------------------------------------ | --------------------------------------------------------------------------- |
| `textlint-rule-ja-hiragana-fukushi`        | 副詞をひらがなで表記させる（例: 「さらに」→「さらに」）                     |
| `textlint-rule-ja-hiragana-hojodoushi`     | 補助動詞をひらがなで表記させる（例: 「〜していただく」→「〜していただく」） |
| `textlint-rule-ja-hiragana-keishikimeishi` | 形式名詞をひらがなで表記させる（例: 「〜すること」→「〜すること」）         |
| `textlint-rule-ja-no-redundant-expression` | 冗長な表現を検出（例: 「〜することができます」→「〜できます」）             |
| `textlint-rule-ja-no-weak-phrase`          | 弱い表現を検出（例: 「〜だと思 います」）                                   |
| `textlint-rule-no-doubled-joshi`           | 二重助詞を検出（例: 「〜についてについて」）                                |
| `textlint-rule-preset-jtf-style`           | JTF日本語標準スタイルガイドに準拠                                           |
| `textlint-rule-prh`                        | 表記ゆれを検出・修正                                                        |
| `textlint-rule-no-todo`                    | TODOコメントを禁止                                                          |
| `textlint`                                 | textlint本体                                                                |

:::

:::details #その他のツール

| パッケージ名             | 役割                                       |
| ------------------------ | ------------------------------------------ |
| `zenn-cli`               | Zennの記事・本をローカルで管理・プレビュー |
| `prettier`               | コードフォーマッター                       |
| `eslint-config-prettier` | PrettierとESLintの競合を防ぐ               |
| `husky`                  | Gitフックを簡単に管理                      |
| `lint-staged`            | ステージングされたファイルのみをチェック   |

:::

## Step 3: Zenn CLI初期化

```bash
npx zenn init
```

これにより、以下のディレクトリとファイルが作成されます。

- `articles/`: 記事を格納
- `books/`: 本を格納
- `.gitignore`: Git除外設定

手動で`images/`ディレクトリーも作成しておくと便利です。

```bash
mkdir images
touch images/.keep
```

## Step 4: textlint・Prettier設定

### 4-1. .textlintrc.json を作成

プロジェクトルートに`.textlintrc.json`を作成します。

:::details .textlintrc.json

```json
{
  "rules": {
    "no-todo": true,
    "no-doubled-joshi": true,
    "ja-hiragana-keishikimeishi": true,
    "ja-hiragana-hojodoushi": true,
    "ja-hiragana-fukushi": true,
    "ja-no-redundant-expression": true,
    "ja-no-weak-phrase": true,
    "preset-jtf-style": {
      "2.1.5.カタカナ": true,
      "2.1.6.カタカナの長音": true,
      "2.2.1.ひらがなと漢字の使い分け": true
    }
  }
}
```

:::

### 4-2. .prettierrc.cjs を作成

プロジェクトルートに`.prettierrc.cjs`を作成します。

:::details .prettierrc.cjs

```javascript
module.exports = {
  overrides: [
    {
      files: [
        '*.ts',
        '*.tsx',
        '*.js',
        '*.jsx',
        '*.mjs',
        '*.cjs',
        '*.css',
        '*.scss',
        '*.less',
        '*.json',
        '*.yml',
        '*.yaml',
        '*.md',
      ],
      options: {
        singleQuote: true,
        trailingComma: 'es5',
        semi: false,
        arrowParens: 'avoid',
        printWidth: 80,
        tabWidth: 2,
        useTabs: false,
      },
    },
  ],
}
```

:::

### 4-3. package.jsonにスクリプトを追加

:::details package.json

```json
{
  "scripts": {
    "dev": "npx zenn preview # ローカルサーバーでプレビューを表示します",
    "lint": "npx textlint **/*.md",
    "lint:fix": "npx textlint **/*.md --fix && prettier --write .",
    "new:article": "npx zenn new:article # 新しい記事を作成します",
    "new:book": "npx zenn new:book # 新しい書籍を作成します"
  }
}
```

:::

:::details #使い方

```bash
npm run lint        # Lintチェック
npm run lint:fix    # 自動修正
npm run dev         # プレビュー
npm run new:article # 記事作成
```

:::

## Step 5: husky・lint-staged設定

### 5-1. huskyを初期化

```bash
npx husky install
```

### 5-2. package.jsonにprepareスクリプトを追加

```json
{
  "scripts": {
    "prepare": "husky install"
  }
}
```

### 5-3. pre-commitフックを作成

```bash
npx husky add .husky/pre-commit "npx lint-staged"
```

`.husky/pre-commit`ファイルが作成されます。以下のように編集します。

:::details .husky/pre-commit

```bash
echo "セキュリティ脆弱性のチェック"
npm audit

echo "ステージングされたファイルに対してlinterを実行する"
npx lint-staged
```

:::

### 5-4. package.jsonにlint-staged設定を追加

:::details package.json

```json
{
  "lint-staged": {
    "*.{md}": ["npm run lint:fix", "npm run lint"]
  }
}
```

:::

これで、コミット時に自動的にMarkdownファイルがチェック・修正され、さらにエラーが残っていないか確認されます。

## Step 6: GitHub Copilot用プロジェクトルール整備

GitHub Copilotを使って記事作成を効率化するため、プロジェクトルールを整備します。

### 6-1. .github/copilot-instructions.md を作成

`.github/copilot-instructions.md`を作成し、プロジェクトのルールを記述します。

:::details .github/copilot-instructions.md

```markdown
# GitHub Copilot Instructions

## プロジェクト概要

このリポジトリは、Zenn連携用のブログコンテンツ管理リポジトリです。

## 技術スタック

- **パッケージ管理**: npm
- **Lint**: textlint (日本語校正)
- **フォーマッター**: Prettier
- **Git Hooks**: husky + lint-staged
- **コンテンツ管理**: Markdown (Zenn連携)

## コーディング規約

### Markdown

- `articles/` ディレクトリ配下に技術記事を配置
- textlintルールに従った日本語表記を使用
  - TODOコメント禁止
  - 二重助詞の禁止
  - 形式名詞・補助動詞・副詞はひらがな表記
  - 冗長表現・弱い表現の禁止

### 命名規則

- ファイル名: 半角英数字（a-z0-9）とハイフン（-）のみ
- 例: `my-first-article.md`

## GitHub Copilotへの指示

### 推奨される動作

- Markdownファイル作成時は必ずtextlintルールに準拠
- 記事作成後は `npm run lint:fix` の実行を提案

### 避けるべき動作

- TODOコメントを含むコードの生成
- 冗長表現・弱い表現の使用
```

:::

### 6-2. .github/prompts/ を作成（オプション）

さらに詳細なプロンプトを作成する場合、`.github/prompts/`ディレクトリーを作成します。

```bash
mkdir -p .github/prompts
```

#### implement.prompt.md（記事実装ガイドライン）

:::details .github/prompts/implement.prompt.md

```markdown
# 記事実装ガイドライン

## 記事作成の基本フロー

1. `articles/` ディレクトリに配置
2. フロントマター設定
3. textlintルールに準拠した本文執筆

## 日本語表記ルール

- 形式名詞・補助動詞・副詞はひらがな表記
- 冗長表現・弱い表現を避ける
```

:::

#### checklist.prompt.md（公開前チェックリスト）

:::details .github/prompts/checklist.prompt.md

```markdown
# 記事公開前チェックリスト

- [ ] フロントマター設定確認
- [ ] コードブロックに言語指定
- [ ] `npm run lint:fix` 実行済み
- [ ] 画像の代替テキスト設定
```

:::

## 実際の運用フロー

1. copilotを使って記事を書く

- 以下の内容を伝えて実装を依頼
  - 記事のテーマ
  - 主要なポイント
  - セクション構成
  - 内容の詳細
  - コード例（必要に応じて）
  - 参考リンク（必要に応じて）

2. 内容を確認
3. copilotに修正・追記・改善を依頼（必要に応じて）
4. 納得いくまで繰り返す
5. 記事完成後、lint実行
   ```bash
   npm run lint:fix
   ```
6. GitHubにコミット・プッシュ
7. Zennに反映されるのを確認

## トラブルシューティング

### textlintのエラーが出る場合

```bash
npm run lint:fix # 自動修正を試す
npm run lint     # 詳細なエラー内容を確認
```

### huskyが動かない場合

```bash
npm run prepare # huskyを再インストール
```

### Zennに反映されない場合

- GitHubの連携設定を確認
- 同期ブランチが正しいか確認
- フロントマターの`published`が`true`になっているか確認

## まとめ

この記事で紹介した設定をすることで、以下のようなメリットが得られます。

- **品質向上**: textlintによる自動校正で日本語の品質が向上
- **効率化**: Prettierによる自動整形で時間短縮
- **安心感**: コミット時の自動チェックで問題を早期発見
- **AI活用**: GitHub Copilotとの連携で執筆がスムーズに

ぜひこの設定を活用して、快適なブログライフをお楽しみください。

## 参考リンク

https://zenn.dev/zenn/articles/connect-to-github

https://zenn.dev/tsukulink/articles/2025-07-how-to-write-techblog-with-zenn
